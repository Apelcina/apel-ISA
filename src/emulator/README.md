Phase 2: functional emulator. Fetch-decode-execute loop over flat memory, register file, minimal memory-mapped console I/O. No caches/pipeline. Machine state (cpu.*), execute for every instruction (execute.*), and the fetch-decode-execute loop (loop.*) are done. Memory-mapped console I/O is not started.
